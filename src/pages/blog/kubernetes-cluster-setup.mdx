# Setting Up Kubernetes Cluster on Ubuntu, macOS, and Windows

Setting up a Kubernetes cluster is essential for running containerized applications at scale. Kubernetes can be installed on various operating systems including Ubuntu, macOS, and Windows. In this guide, we will walk you through the steps for each operating system.

## Table of Contents

1. [Setting Up Kubernetes on Ubuntu](#setting-up-kubernetes-on-ubuntu)
2. [Setting Up Kubernetes on macOS](#setting-up-kubernetes-on-macos)
3. [Setting Up Kubernetes on Windows](#setting-up-kubernetes-on-windows)

---

## Setting Up Kubernetes on Ubuntu

To set up Kubernetes on Ubuntu, we will use **Kubeadm**, which is a tool for easily setting up a Kubernetes cluster.

### Prerequisites:
- Ubuntu 20.04 or later
- At least 2GB of RAM
- A working internet connection

### Steps:

### 1. Install Docker
Kubernetes uses Docker as its container runtime. To install Docker on Ubuntu, follow these steps:

```bash
sudo apt update
sudo apt install apt-transport-https ca-certificates curl software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
sudo apt update
sudo apt install docker-ce


### 2. Install Kubectl kubeadm Kubelet

```bash
sudo apt update
sudo apt install -y apt-transport-https
curl -s https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
sudo apt-add-repository "deb https://apt.kubernetes.io/ kubernetes-xenial main"
sudo apt update
sudo apt install -y kubelet kubeadm kubectl

### 3. Disable swap
```bash
sudo swapoff -a

### 4. Init kubernetes cluster
```bash
sudo kubeadm init --pod-network-cidr=10.244.0.0/16


### 5. Set Up kubeconfig for kubectl
```bash
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config


### 6. Install network
```bash
kubectl apply -f https://raw.githubusercontent.com/coreos/flannel/master/Documentation/kube-flannel.yml


# Installing Kubernetes on macOS

In this guide, we will set up a local Kubernetes cluster on macOS using **Minikube**. Minikube is a tool that runs a single-node Kubernetes cluster inside a virtual machine on your local machine. It is great for testing and development purposes.

## Table of Contents

1. [Prerequisites](#prerequisites)
2. [Installing Homebrew](#installing-homebrew)
3. [Installing Minikube](#installing-minikube)
4. [Starting Minikube Cluster](#starting-minikube-cluster)
5. [Using kubectl with Minikube](#using-kubectl-with-minikube)
6. [Accessing Kubernetes Dashboard](#accessing-kubernetes-dashboard)
7. [Stopping and Deleting Minikube](#stopping-and-deleting-minikube)

---

## Prerequisites

- macOS 10.12 or later
- **Homebrew** package manager installed
- Virtualization support (Minikube uses VirtualBox or HyperKit for VM creation)

You’ll also need **kubectl**, the Kubernetes command-line tool, to interact with the Kubernetes cluster.

---

## Installing Homebrew

Homebrew is a popular package manager for macOS that simplifies the installation process. If you don't have Homebrew installed, run the following command:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"



# Installing Kubernetes on Windows

This guide will walk you through the steps to set up a local Kubernetes cluster on Windows using **Minikube**. Minikube provides an easy way to run Kubernetes clusters locally, and it can be used to create a single-node Kubernetes cluster for development and testing purposes.

## Table of Contents

1. [Prerequisites](#prerequisites)
2. [Installing Chocolatey](#installing-chocolatey)
3. [Installing Minikube](#installing-minikube)
4. [Starting Minikube Cluster](#starting-minikube-cluster)
5. [Using kubectl with Minikube](#using-kubectl-with-minikube)
6. [Accessing Kubernetes Dashboard](#accessing-kubernetes-dashboard)
7. [Stopping and Deleting Minikube](#stopping-and-deleting-minikube)

---

## Prerequisites

Before installing Minikube on Windows, ensure that the following are available on your system:

- **Windows 10** or later (64-bit)
- **Virtualization** enabled in BIOS (Hyper-V or VirtualBox)
- **Windows Subsystem for Linux (WSL)** is enabled if using Hyper-V as the driver
- **Chocolatey** package manager (for easy installation)

---

## Installing Chocolatey

**Chocolatey** is a package manager for Windows, which allows you to install software quickly and easily. To install **Chocolatey**:

1. Open **PowerShell** as **Administrator**.
2. Run the following command to install Chocolatey:

```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.SecurityProtocolType]::Tls12; iex ((New-Object System.Net.WebClient).DownloadString('https://chocolatey.org/install.ps1'))


### After installation completes, verify that Chocolatey is installed by running:
```bash
choco --version



